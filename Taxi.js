class Personnage {
    constructor(name,Mental,nbTaxi) {
        this.Mental = Mental
        this.name = name
        this.nbTaxi = nbTaxi
    }
}

class Musique {
    constructor(nameM) {
    this.nameM = nameM
}
    changetaxi(){

        if (Musique == [0]) {
            Mental = (Mental - 1);
            nbTaxi = (nbTaxi + 1);
            Feurouge = (Feurouge - 1)
                return true;
            }
    
            return false;
        }
}

let John = new Personnage(John,10,0);
let Musique = ["Annissa", "VetV", "Ricadendoti", "VinItalien", "Hummanoide"];
let Feurouge = 30;

while(Mental > 0 & this.changetaxi = true) {

    John.changetaxi("John prend un autre taxi, sacré Annissa !")

};

if(Mental <= 0){
    alert("explosion")
};

